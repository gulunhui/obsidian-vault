#选择题 
## 主频
时钟频率，很大程度上决定了计算机的运算速度。主时钟不断产生固定频率的时钟脉冲。

## CPU时钟周期
时钟周期是计算机中最基本的单位基准时间。主频的倒数

## 机器周期
CPU周期，一个基本操作所需要的时间就是一个机器周期。
一个机器周期由若干时钟周期组成。
一个指令周期有包含若干机器周期。

## CPI
每条指令执行所用的时钟周期数
**是一个平均值**
IPC（每个时钟周期执行的指令条数）IPC等于CPI的倒数

## MIPS
每秒百万条指令的执行条数。
MIPS=指令条数/执行时间* 10^6=主频/CPI=主频* IPC
