# 概述
微服务是一种架构风格，将单体应用划分为小服务，服务之间相互协作。服务之间采用轻量级通信机制，每个服务围绕业务能力进行构建
# 优点
1. 分解巨大的单体应用为多个服务方法，解决了复杂性问题。功能不变，但是并发提到提升
2. 每个服务能够独立开发
3. 每个微服务可以独立部署。
4. 每个服务独立扩展
# 面临的挑战
1. 并非所有服务都能转为微服务
2. 部署较以往架构更加复杂
3. 性能问题，可能产生延迟或调用出错
4. 数据一致性：保持数据一致性有困难