#数据中台2_1
* 支持columns，同时支持切片键
* 最新版本war包中包含了，datax的jar，需要验证，当datax已经在共享目录下时，是不是能够直接导入datax.app就可以使用
* 验证datax是否部署成功，集群验证。
* 动态增加插件
* datax部署状态界面，包括检测一共有多少个数据库插件（增加一个测试job），core.json配置的读取大小配置。
* 正在执行的datax列表，支持一个shutdown的方法