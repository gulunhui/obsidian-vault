#数据中台 
在标准表的规则编辑界面，选择了实例表后 列表中展示的字段名，要从标准表上获取吗？
这里有两种情况：
模型字段名为英文
模型字段名为中文，但是和标准字段描述不一致。
当不一致时取哪一个？

关于规则编辑界面如果规则执行出现异常，要将异常抛出并且展示。对于标准的规则，在选择完实例后，增加一步字段对比分析，如果规则中涉及的字段在标准表中不存在，要给出提示。

标准字段列表中，如果某字段不是主键，不显示为否，为空值。允许为空，默认为是

